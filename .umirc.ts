import { defineConfig } from 'dumi';

export default defineConfig({
  title: '前端标配',
  mode: 'site',
  favicon: '/km@2x.png',
  description: '本系列以 web 前端从业技术为导向, 致力于输出一个标准前端技能 map',
  locales: [['zh-CN', '中文']],
  logo: '/logo.png',
});
